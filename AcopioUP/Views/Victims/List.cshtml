@using AcopioUP.Models
@model IEnumerable<Victim>
@{
    ViewBag.Title = "ReadOnlyList";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Víctimas</h2>

<table id="victims" class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Apellidos</th>
            <th>Email</th>
            <th>Eliminar</th>
        </tr>
    </thead>
    <tbody></tbody>
</table>

@section scripts
{
    <script>
        $(document).ready(function () {
            var table = $("#victims").DataTable({
                ajax: {
                    url: "/api/victims",
                    dataSrc: ""
                },
                columns: [
                    {
                        data: "firstName",
                        render: function (data, type, victim) {
                            return "<a href='/victims/edit/" + victim.id + "'>" + victim.firstName + "</a>";
                        }
                    },
                    {
                        data: "lastName",
                        render: function (data, type, victim) {
                            return "<a href='/victims/edit/" + victim.id + "'>" + victim.lastName + "</a>";
                        }
                    },
                    {
                        data: "email",
                        render: function(data, type, victim) {
                            return "<a href='/victims/edit/" + victim.id + "'>" + victim.email + "</a>";
                        }
                    },
                    {
                        data: "id",
                        render: function(data) {
                            return "<button class='btn-link js-delete' data-victim-id=" + data + ">Delete</button>";
                        }
                    }

                ]
            });

            $("#victims").on("click", ".js-delete", function () {
                var button = $(this);
                bootbox.confirm("Estas seguro que quieres eliminar esta victima?", function(result) {
                    if (result) {
                        $.ajax({
                            url: "/api/victims/" + button.attr("data-victim-id"),
                            method: "DELETE",
                            success: function () {
                                table.row(button.parents("tr")).remove().draw();
                            }
                        });
                    }
                });
            });
        });
    </script>
}
